lib-auth {
  password {
    pepper = "pepper"
    pepper = ${?LIB_AUTH_PASSWORD_PEPPER}
  }

  jwt {
    secret = "secret"
    secret = ${?LIB_AUTH_JWT_SECRET}

    issuer = "companies-board"
    issuer = ${?LIB_AUTH_JWT_ISSUER}
  }

  csrf {
    secret = "secret"
    secret = ${?LIB_AUTH_CSRF_SECRET}
  }
}
