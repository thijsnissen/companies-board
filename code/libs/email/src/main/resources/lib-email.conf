lib-email {
  host = "localhost"
  host = ${?LIB_EMAIL_HOST}

  port = 1025
  port = ${?LIB_EMAIL_PORT}

  username = "docker"
  username = ${?LIB_EMAIL_USERNAME}

  password = "docker"
  password = ${?LIB_EMAIL_PASSWORD}

  from = "noreply@companies.board"
  from = ${?LIB_EMAIL_FROM_ADDRESS}
}
